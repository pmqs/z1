name: Debian

# Set to manual execution only for now.
on:
  workflow_dispatch:
  # push:
  # pull_request:

jobs:
  configure:
    name: ${{ matrix.compiler.name }} + ${{ matrix.arch }} + ${{ matrix.compiler.tag }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

      matrix:
        arch:
        - amd64
        # - i386

        compiler:

            # https://hub.docker.com/_/debian/tags
            # https://hub.docker.com/r/debian/eol/tags

        # - name: gcc
        #   tag: experimental
        # - name: clang
        #   tag: experimental




        # WORKING
        # - name: gcc-13
        #   tag: debian:trixie-slim
        # - name: gcc-12
        #   tag: bookworm
        # - name: gcc-11
        #   tag: bookworm
        # - name: gcc-10
        #   tag: bullseye
        # - name: gcc-9
        #   tag: bullseye
        # - name: gcc-8
        #   tag: buster
        # - name: gcc-7
        #   tag: buster





        # WORKING
        # - name: clang-17
        #   tag: trixie
        # - name: clang-16
        #   tag: bookworm
        # - name: clang-15
        #   tag: bookworm
        # - name: clang-14
        #   tag: bookworm
        # - name: clang-13
        #   tag: bookworm
        # - name: clang-11
        #   tag: bullseye
        # - name: clang-9
        #   tag: bullseye
        # - name: clang-7
        #   tag: buster


        # NOT WORKING

        - name: gcc-6
          tag: debian/eol:stretch-slim

        # - name: gcc-5
        #   tag: debian/eol:stretch

        # - name: gcc-4
        #   tag: debian/eol:stretch

        # - name: clang-12
        #   tag: debian/eol:stretch

        # - name: clang-10
        #   tag: debian/eol:stretch


        # - name: clang-8
        #   tag: debian/eol:stretch

        # - name: clang-6
        #   tag: debian/eol:stretch



    container:
    #   image: ${{ matrix.arch }}/${{ matrix.compiler.tag }}
      image: debian/eol:stretch-slim@sha256:769c8ce77cee9100b12dec0af9a4cfba0b35097fabbbd65a2898e5ca72893a52

    steps:

    - name: Checkout repository
      uses: actions/checkout@v1
    #   with:
        # show-progress: 'false'

    # - name: Install packages
    #   if: matrix.compiler.repo
    #   run: |

    #     echo "deb http://archive.debian.org/debian stretch main contrib non-free" > /etc/apt/sources.list
    #     echo "deb http://archive.debian.org/debian-archive/ oldstable main contrib non-free" >> /etc/apt/sources.list
    #     echo "deb-src http://archive.debian.org/debian-archive/ oldstable main contrib non-free" >> /etc/apt/sources.list

    - name: whoami
      run: id

    - name: Install packages
      run: |
            apt-get update -qq
            apt-get install -yq make ${{ matrix.compiler.name }}

    - name: Display ${{ matrix.compiler.name }} version
      run: ${{ matrix.compiler.name }} -v

    - name: Display uname
      run: |
        uname -a
        uname -m

    - name: configure (unix/Makefile)
      run: |
        make -j2 -f unix/Makefile generic CC=${{ matrix.compiler.name }} LOCAL_ZIP="-Werror" ${{ matrix.opts }}
        cat flags

    - name: Run zip -v
      run: ./zip -v


    # - name: build (unix/Makefile)
    #   run: |
    #         make -j2 -f unix/Makefile generic

    # - name: Run zip -v
    #   run: ./zip -v

    # - name: Run zipcloak -v
    #   run: ./zipcloak -v

    # - name: Run zipnote -v
    #   run: ./zipnote -v

    # - name: Run zipsplit -v
    #   run: ./zipsplit -v

